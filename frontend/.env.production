# Base URL of the deployed backend (no trailing slash, no /api suffix — that's added in code).
# Replace with your actual Render backend URL after deploying it.
# This value is also overridable from the Vercel project's Environment Variables settings,
# which takes precedence over this file during Vercel's build.
VITE_API_URL=https://your-render-backend-url.onrender.com
